package us.jbec.lct.models;

/**
 * Field names used in ImageJob file. This was originally used to provide interoperability between newer
 * and older versions of the application, but probably is not needed anymore
 */
public enum ImageJobFields {
    NOTES
}
